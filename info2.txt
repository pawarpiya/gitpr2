//git status

//1st step

//git init

//working ====> staging ====> local repo

                                                             //remote repo


git - status ------> red untracked

git add . =====> working to staging

git commit -m "loging feature" =====> staging to local repo

git config --global user.email "ppriyanka160998@gmail.com"
  git config --global user.name "pawarpiya"

//git hub (login to git hub)
//create new repo


git branch -M main
git remote add origin https://github.com/pawarpiya/gitpr3.git
git push -u origin main